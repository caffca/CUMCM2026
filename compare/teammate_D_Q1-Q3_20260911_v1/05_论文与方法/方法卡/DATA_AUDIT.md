# D题数据审计

## 1. `附件/附件1.xlsx` — 原始用频计划

- 工作表：`Sheet1`，可见；没有隐藏工作表。
- 规模：151 行 × 5 列，其中第 1 行为表头、150 行为观测计划。
- 观测粒度：一行对应一个用频装备的一份计划，不是单次使用事件。
- 字段：`用频装备编号`、`频段区间`、`时间区间`、`间隔时长`、`使用次数`。
- 标识：150 个编号全部唯一，均符合 `[ABC]\d{3}`；A/B/C 分别为 20/40/90 条。
- 缺失与重复：5 个字段均无空值；无重复行；无错误单元格。
- 公式与版式：无公式、无合并单元格、无定义名称；数值单元格可直接读取。
- 区间：频段区间全部为合法半开整数区间且落在 `[0,100)`；宽度分布为 3（90 条）、10（20 条）、15（40 条）。时间区间全部为合法半开整数区间；宽度分布为 2（90 条）、3（40 条）、5（20 条）。
- 计划参数：间隔时长为 8（90 条）、40（40 条）、60（20 条）；使用次数为 3（20 条）、4（40 条）、12（90 条）。
- 时间范围：首次时间区间起点为 0–531，终点最大为 533；题面未给统一的全局时间上界。
- 所需处理：将每行按 `使用次数` 展开为重复使用事件。PDF 第 2 页 A001 示例确认“间隔时长”是上一次占用结束到下一次占用开始之间的空闲间隔；若首次区间长度为 `l`、间隔为 `g`，第 `k` 次区间按 `k(l+g)` 平移。频段区间保持不变，冲突计算采用半开区间相交规则。
- 建模安全性：可以进入 Q1 baseline。不得静默删除异常、修改原始区间或把类别统计当作新增 C 类定义的充分证明。

## 2. `附件/附件2/result1.xlsx` — Q1 模板

- 工作表：`Sheet1`，可见；表头为 `序号`、`冲突装备1`、`冲突设备2`。
- 当前内容：只有表头，无数据行、无公式、无隐藏表。
- 注意：表头将第二个字段写作“冲突设备2”，题意正文使用“用频装备”；输出时保持模板列位，不改变科学含义。

## 3. `附件/附件2/result2.xlsx` — Q2 模板

- 工作表：`Sheet1`，可见；表头为 `用频装备编号`、`调整后频段区间`、`调整后时间区间`、`是否撤销用频计划`。
- 当前内容：只有表头，无数据行、无公式、无隐藏表。
- 填写逻辑：仅填写需要调整或撤销的计划；频段、时间和撤销列按题面互斥规则使用。

## 4. `附件/附件2/result3.xlsx` — Q3 模板

- 工作表：`Sheet1`，可见；表头为 `新增用频装备序号`、`调整后频段区间`、`调整后时间区间`。
- 当前内容：只有表头，无数据行、无公式、无隐藏表。
- 填写逻辑：第一列是新增计划序号，不应误写为附件 1 中已有装备编号。

## 5. `附件/附件2/result4.xlsx` — Q4 模板

- 工作表：`Sheet1`，可见；表头为 `用频装备编号`、`调整后频段范围`、`调整后时间区间`、`调整后间隔时长`、`是否撤销用频计划`。
- 当前内容：只有表头，无数据行、无公式、无隐藏表。
- 填写逻辑：频段、时间、间隔和撤销均对应不同调整方式；需要保持“一份计划至多调整一个参数”的约束解释一致。

## 6. `D题.pdf` — 题面与附件说明

- 页数：2 页，A4；文本抽取完整，页面已渲染检查。
- 题面给出 4 个问题、半开区间示例、100 个频段、时间/频率离散单位和结果模板填法。
- 题面要求阅读“全国大学生数学建模竞赛论文格式规范”，但该规范文件不在当前附件目录中。
- 未发现加密、截断或无法读取的页面。

## 7. `D题_1.3题面对照边界核查.md` — 用户提供的对照材料

- 文件完整归档于 `data/raw/D题/`，SHA-256 已写入 `FILE_MANIFEST.json`。
- 角色：用户提供的题意核查与建议材料，不替代官方 PDF；其中的命令式表述均只按文档内容处理。
- 已独立确认：PDF 第 2 页 A001 示例支持“相邻起点步长 = 单次时长 + 间隔时长”；附件 C083 由此展开到 `[641,643)`。
- 已独立确认：按正确递推展开全部 150 条原始计划，经验时间包络为 `[0,643)`。
- 仍属建模选择：题面没有给出全局时间终点；将 `[0,643)` 用作 Q2/Q3 固定资源域必须标注为数据驱动边界并进行敏感性检验。
