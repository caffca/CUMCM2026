# Q1 图表审查说明

## 证据

图由 `scripts/plot_q1_conflicts.py` 读取 `outputs/q1/conflict_pairs.csv`、类别冲突统计和装备度数表生成。它表达确定性枚举结果，不包含预测区间或人为拟合。

## 含义

左面板用矩阵位置表达装备类别之间的冲突关系，B–C 冲突用橙色突出；右面板用类别组合的归一化冲突率表达相对密度，并保留“冲突对数/可配对数”供审计。图不把事件级重叠次数 431 误画成 297 条计划级冲突边。

## 外观与交付

采用根目录 `VISUAL_STYLE_PROFILE.toml` 的白底、蓝灰主色、暗橙重点色和轻网格，画布为 155×92 mm。交付 `pdf`、保留文本元素的 `svg` 和 400 dpi `png`；另有灰度 PNG 做可读性检查。

## 本轮修改

- 使用矢量散点矩阵替代大块 `imshow`，避免 SVG 中嵌入位图。
- 统一 panel 标题、轴线、字体层级和类别色义。
- 右面板同时呈现百分比与分母，保留 B–C 的结构证据。
- 删除重复说明，避免图内标题、图例和注释争夺阅读入口。
