# Q2 图表审查说明

## 证据

图由 `scripts/plot_q2_tradeoff.py` 读取冻结的六撤销 `results.json`、主目标证明和优先级前缀运行，并由 `validation.json` 支撑无冲突口径。

## 含义

左面板把 150 个计划按类别拆成“保留/调整/撤销”。右面板直接列出字典序前缀的上下界：总撤销和 A 类撤销已闭合，B 类撤销及调整数仍是上界。

## 外观与交付

采用根目录 `VISUAL_STYLE_PROFILE.toml` 的白底、蓝灰/蓝绿/陶土红状态色和轻网格，画布为 155×92 mm。交付 PDF、SVG、400 dpi PNG 与灰度 PNG。彩色预览已实际检查。

## 本轮修改

- 用六撤销正式结果替换旧 19 撤销历史图。
- 新增逐层 LB/UB 与证明状态，避免把后续 incumbent 误写成最优。
- 保持 Q2 图不消费任何 Q3 数据。
