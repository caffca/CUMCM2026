# D 题 Q1–Q3 队友对比包

这是一份面向队友阅读、比较和复现的独立资料包。它对应本机仓库
`C:\Users\ysw\Desktop\CUMCM2026` 在 `main` 分支的提交
`d8f7c52e60c18860a170b4496fbfd3912f6a40e6`，并包含当前工作区已经生成的新版 Q1/Q2/Q3 图表。

## 先看什么

不需要运行代码时，按下面顺序阅读：

1. `00_快速导航/核心结论与比较指南.md`
2. `outputs/q1/summary.md`、`outputs/q2/summary.md`、`outputs/q3/summary.md`
3. `paper/draft_sections/q1.md`、`q2.md`、`q3.md`
4. 每个问题目录下的 `validation.json`、图表和 Excel 结果

需要核对题意时，再看 `01_题目与原始数据/`；需要复现时，看
`00_快速导航/运行与验证说明.md`。

## 当前正式口径

| 问题 | 当前正式结论 | 结论边界 |
|---|---:|---|
| Q1 冲突检测 | 297 对冲突装备；431 次事件级重叠 | 原始 150 个计划完整展开，半开区间判定 |
| Q2 冲突消解 | 最少撤销 6 个；当前方案调整 126 个；144 个计划活动 | `(R,R_A,R_B,M,M_A,M_B)=(6,0,4,126,16,34)` 已闭合；末级平移量 775 仅是可行上界 |
| Q3 新增 C 类 | 最多新增 141 台；合并后 285 个活动计划 | 只对批准的 Q2 六撤销方案和 `[0,643)×[0,100)` 资源域成立 |

## 最容易混淆的旧结果

仓库历史上曾有“Q2 撤销 19 个、Q3 新增 203 台”的方案。它对应另一份 Q2 背景，已经被当前六撤销方案取代；本包不把它作为正式结论。Q2 的 C 类撤销数为 0 是当前方案的结果结构，不是题目规定。

## 运行环境

- Windows + Python 3.12（建议用 `py -3.12`）
- 依赖见 `requirements-cumcm.txt`
- 未打包虚拟环境、Git 历史、缓存、参考论文全文或凭据
- Q2 的完整证明依赖 OR-Tools；SAT 证明证据已作为 JSON 随包提供
- `result1.xlsx`、`result2.xlsx`、`result3.xlsx` 是已经生成并核验的正式结果表，复现时不必重新生成

## 重要边界

- 643 来自附件数据展开得到的时间包络，不是题面直接给定的全局终点。
- Q2 的最后一个目标 `S10=775` 没有宣称全局最优；不要把整条字典序链写成完全最优。
- Q3 固定 Q2，不反向修改 Q2；若 Q2 方案或时间边界变动，Q3 必须重算。
- Q4 尚未建模，本包只保留 `result4.xlsx` 原始模板供后续接续。

## 本包的来源状态

打包时生产仓库存在未提交的新版图表和绘图脚本修改；这些修改已纳入本包，但不改变上表的 Q1–Q3 核心数值。详细文件清单和 SHA-256 见 `PACKAGE_MANIFEST.json`。

