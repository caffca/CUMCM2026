# Q2-R32 开发/子预算运行登记（DEV LOG）

> 协议要求"失败/超时也必须写盘、禁止删失败记录"。本路线只有一次权威运行（确定性，1 重复），
> 但开发期有两次**会产出假可行解**的实现缺陷被哨兵抓到，登记如下（它们不产生 result.json，
> 因为从未成为候选解；不登记就等于隐瞒）。

| # | 阶段 | 现象 | 根因 | 处置 |
|---|---|---|---|---|
| A | 阶段 C 谓词 | `violated()` 在任何动作组合下都返回 0 条残留冲突 ⇒ 取整后"零冲突"，修复层空转，直接把未消解方案当可行解交出去 | G\* 子句存的是 `(ai,aj)` **元组**集合，而状态编码是 `ai*64+aj` **整数**，成员测试恒 False | 由 `code/test_sentinels.py` 的用例 2（全恒等 Φ=2124>0 却"零冲突"）抓出；改为统一编码集合，哨兵现在断言"Φ>0 ⇔ 冲突>0"。 |
| B | 阶段 A 梯度 | 解析次梯度与中心差分方向不一致（个别分量符号相反） | 边级聚合数组（per-edge）与点对级数组（per slot-pair）混用：`np.add.at(Gdt, plo, GTi*ovb)` 形状广播错误 | 改为"先按 eidx 聚合到边级，再按 lo/hi 端点 bincount 聚合到计划级"；`gradient_vs_fd_worst_mismatch=0.0`。 |
| C | 阶段 B 取整 | 计划被分配到 `df=0` 或越界动作 ⇒ evaluator 会报 `empty_action/out_of_resource` | 取整盒未按 R3 逐计划收紧（`-f0 ≤ δf ≤ 100-f1`、`-t0 ≤ δt ≤ 643-末窗结束`） | 盒改为逐计划上下界数组，取整后再裁剪；输出只列被改/撤销者。 |
| **权威** | 全流程 | Φ 2124→672.18；取整后残留 297→175；1 轮 4 个分量 CP-SAT 修复后残留 0；去撤销阶梯 28→6 | — | **391.07s**，evaluator `feasible=true`，objective `[6,123,2205,762]`，`n_violations=0`。 |

## 该路线的诚实定性
1. **引导有效但不足以收口**：连续层把残留冲突对从 297 压到 175（-41%），这一步是真的
   （不是随机取整能达到的水平）；但 Φ_final=672≫0，且取整后残留图是单一巨分量（130/150 顶点），
   ⇒ "局部修复"的第一个子问题就是 `free=150 / clauses=270626` 的**全量模型**
   ⇒ 命中 route 预设降级条件（覆盖 >50% ⇒ 引导无增量），最终解的质量实际来自 CP-SAT。
2. **无任何证明贡献**：去撤销阶梯的自由集覆盖 147/150，最后一轮 `Σr≤5` 只到 UNKNOWN，
   不构成证书 ⇒ `lower_bound/upper_bound = null`（任务书规定）。
3. **确定性只覆盖连续层**：次梯度部分严格确定性（固定迭代序/步长规则/破平键=计划编号）；
   修复层 CP-SAT `num_search_workers=4` 的并行组合策略可能引入 run-to-run 差异 ⇒
   本路线的"确定性"评级应记为 **部分确定（连续层确定，离散修复层不保证逐位一致）**，
   未做二次重跑验证（预算优先给权威运行）。
